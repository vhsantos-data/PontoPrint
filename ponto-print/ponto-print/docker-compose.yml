services:
  site:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DB_PATH: /app/data/pontoprint.db
    volumes:
      - ./data:/app/data
